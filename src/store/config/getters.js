export const secretLength = state => state.secretLength;
export const minSecretLength = state => state.minSecretLength;
export const maxSecretLength = state => state.maxSecretLength;

export const symbols = state => state.symbols;
export const minSymbols = state => state.minSymbols;
export const maxSymbols = state => state.maxSymbols;

export const attemptsLimit = state => state.attemptsLimit;
export const attempts = state => state.attempts;
export const minAttempts = state => state.minAttempts;
